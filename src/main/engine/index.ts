export { DataEngine, type EngineSnapshot } from './DataEngine';
export { claudeDir } from './paths';
// M3 (additive): on-demand transcript detail for the renderer's drawer.
export { getTranscriptDetail } from './transcriptDetail';
